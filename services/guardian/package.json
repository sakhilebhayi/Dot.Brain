{
  "name": "@dot-brain/guardian",
  "version": "1.0.0",
  "description": "Dot ecosystem production guardian -- polls each platform's dot-guardian/v1 health endpoint, opens and dedupes incidents, decides via severity/confidence/risk within progressive-autonomy levels, remediates through the platform's own GitHub repo and CI/CD workflow, verifies after deploy, rolls back on failure, and records every incident to Dot.Memory. See README.md for the platform contract.",
  "type": "module",
  "bin": {
    "guardian": "./src/cli.js"
  },
  "scripts": {
    "test": "node --experimental-sqlite --test"
  },
  "engines": {
    "node": ">=22.5.0"
  }
}
